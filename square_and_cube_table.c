#include <stdio.h>

int main(void) {
  int number;
  int square;
  int cube;

  puts("number\tsquare\tcube");
  number = 0;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 1;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 2;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 3;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 4;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 5;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 6;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 7;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 8;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 9;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

  number = 10;
  square = number * number;
  cube = number * number * number;
  printf("%d\t%d\t%d\n", number, square, cube);

}
